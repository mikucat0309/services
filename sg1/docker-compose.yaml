services:
  caddy:
    image: ghcr.io/mikucat0309/caddy:2.11.4
    volumes:
      - ./caddy:/etc/caddy:ro
      - caddy_data:/data
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
    restart: unless-stopped
    cap_add:
      - NET_BIND_SERVICE
    cap_drop:
      - ALL

  tinyauth:
    image: ghcr.io/tinyauthapp/tinyauth:v5.1.3
    environment:
      TINYAUTH_CONFIGFILE: /config/config.yaml
    volumes:
      - ./tinyauth/config:/config:ro
      - tinyauth_data:/data
    restart: unless-stopped
    user: tinyauth
    cap_drop:
      - ALL

volumes:
  caddy_data:
  tinyauth_data:
